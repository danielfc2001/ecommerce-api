/* import supertest from "supertest";
import app from "../index.js";

const api = supertest(app);

test("POST /api/users/register", async () => {
  const newUser = {
    email: "hT0o4@example.com",
    username: "test",
    password: "test",
    role: "user",
  };
}); */
